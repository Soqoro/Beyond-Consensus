# Candidate Linux GPU stack: official source support inspected; GPU UNTESTED here.
# Install once during setup. Select a site/driver-compatible PyTorch wheel index.
# Do not install or upgrade dependencies from batch jobs.
torch==2.10.0
torchvision==0.25.0
transformers==5.3.0
huggingface-hub==1.5.0
safetensors==0.7.0
tokenizers==0.22.2
Pillow==12.1.1
Jinja2==3.1.6
